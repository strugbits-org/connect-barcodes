[build]
builder = "nixpacks"

[build.nixpacks]
buildCommand = "npm install --prefer-offline && npx medusa build"

[deploy]
startCommand = "npx medusa start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
