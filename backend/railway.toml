[build]
builder = "nixpacks"

[build.nixpacks]
buildCommand = "npm ci --prefer-offline && npx medusa build"

[deploy]
startCommand = "node .medusa/server/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
